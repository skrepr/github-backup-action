{
  "name": "github-backup-action",
  "version": "1.0.0",
  "description": "Github action for backing up Github orgs with migrations",
  "main": "action.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "esbuild action.ts --platform=node --target=node16 --bundle --outfile=dist/action.js",
    "watch": "esbuild action.ts --platform=node --target=node16 --bundle --outfile=dist/action.js --watch",
    "serve": "yarn build && node app.js"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/skrepr/github-backup-action.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/skrepr/github-backup-action/issues"
  },
  "homepage": "https://github.com/skrepr/github-backup-action#readme",
  "dependencies": {
    "@actions/core": "^1.8.2",
    "@actions/github": "^5.0.3",
    "@octokit/core": "^3.6.0",
    "@types/node": "^17.0.41",
    "aws-sdk": "^2.1150.0",
    "dotenv": "^16.0.1",
    "esbuild": "^0.14.43",
    "fs": "^0.0.1-security"
  }
}
